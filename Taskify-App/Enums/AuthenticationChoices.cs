namespace Taskify_App.Enums
{
    /// <summary>
    /// Enum to store the authentication choices.
    /// </summary>
    public enum AuthenticationChoice
    {
        Login = 1,
        Register = 2,
        Exit = 3,
    }
}