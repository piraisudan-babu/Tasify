namespace Taskify_App.Enums
{
    /// <summary>
    /// Enum to store the task operations.
    /// </summary>
    public enum TaskOperations
    {
        TaskManager = 1,
        SummaryView = 2,
        Export = 3,
        TimeTracker = 4,
        Logout = 5,
    }
}
